<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Book Search</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background-image: linear-gradient(to bottom left, #e6f2fe, #5dabff);
      margin-top: 0px;
    }

    h1 {
      margin-bottom: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    label,
    input,
    select,
    button {
      margin-bottom: 10px;
      width: 250px;
    }

    button {
      padding: 8px;
      border-radius: 3px;
      border: none;
      background-color: #007bff;
      color: white;
      cursor: pointer;
    }

    input[type='text'],
    select {
      width: 250px;
      padding: 8px;
      border-radius: 3px;
      border: 1px solid #ccc;
    }
  </style>
</head>
<body>
  <h1>Book Finder</h1>
  <% if (userName) { %>
    <p>Welcome, <%= userName %>! Happy searching.</p>
  <% } %>
  <p>Enter a book title or keyword then select the database you want to search.</p>
  <form action="/search" method="post">
    <label for="keyword">Enter Keyword:</label>
    <input type="text" id="keyword" name="keyword" placeholder="Enter all to see all books" required />
    <label for="database">Choose Database:</label>
    <select id="database" name="database">
      <option value="postgres">PostgreSQL</option>
      <option value="mongodb">MongoDB</option>
      <option value="both">Both</option>
    </select>
    <button type="submit">Search</button>    
  </form>  
  <% if (userName) { %>
    <form action="/logout" method="POST">
      <button type="submit">Logout</button>
    </form>
  <% } %>
</body>
</html>



